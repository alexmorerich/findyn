name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  serving:
    name: Serving plane (Workers / D1)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: serving
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            serving/package-lock.json
            dashboard/package-lock.json

      - run: npm ci

      # The Worker serves the dashboard from its assets binding, so
      # dashboard/dist must exist before the bundle can be validated.
      - name: Build dashboard assets
        working-directory: dashboard
        run: npm ci && npm run build

      - name: Binding types match wrangler.jsonc
        run: |
          npm run types
          git diff --exit-code worker-configuration.d.ts \
            || { echo "::error::worker-configuration.d.ts is stale — run 'npm run types' and commit"; exit 1; }

      - name: Typecheck
        run: npm run typecheck

      - name: Cron/config drift check
        run: npm run check:crons

      - name: Tests (applies D1 migrations to an isolated database)
        run: npm test

      - name: Validate deployable bundle
        run: npm run deploy:check

  dashboard:
    name: Dashboard (Astro)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

  compute:
    name: Compute plane (Python)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: compute
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: compute/pyproject.toml

      - run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check .

      - name: Format check
        run: ruff format --check .

      - name: Tests
        run: pytest --cov=findyn --cov-report=term-missing
