{
  "name": "findyn-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "FinDyn v1.0 dashboard — static Astro site over the public read-only API (see FINDYN_V1_SPEC.md §16)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "astro check && tsc --noEmit"
  },
  "dependencies": {
    "astro": "^5.16.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.7.3"
  }
}
